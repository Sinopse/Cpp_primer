reference parameters should be refs to const if we do not desire to change the underlying object. We might get a compile time error if we have functons in the code that take in the same parameter if it should be const for one func and non const for another
