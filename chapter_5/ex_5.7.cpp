#include <iostream>

int main() {

  int ival1, ival2;


  // case (a)
  if (ival1 != ival2)
    ival1 = ival2;
  else
    ival1 = ival2 = 0;

  //case (b)
  if (ival < minval) {
    minval = ival;
    occurs = 1;
  }

  //case (c)
  if (int ival = get_value())
    std::cout << "ival = " << ival << std::endl;

  else (!ival)
    std::cout << "ival = 0\n";

  //case (d)
  if (ival == 0) // or if (!ival)
    ival = get_value();

